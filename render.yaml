services:
  - type: web
    name: plant-disease-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 5000
      - key: MODEL_PATH
        value: plant_disease.tflite
      - key: LABELS_PATH
        value: labels.txt
