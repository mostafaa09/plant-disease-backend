version: '3.8'

services:
  plant-disease-api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MODEL_PATH=plant_disease.tflite
      - LABELS_PATH=labels.txt
    restart: unless-stopped
